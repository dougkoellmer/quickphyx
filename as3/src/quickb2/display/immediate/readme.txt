Immediate-mode rendering means you call a function, and it results in an immediate draw to a buffer.

Often it also means that you clear the screen and redraw every frame as well.

Here you'll find some interfaces and partial convenience implementations that focus on that.